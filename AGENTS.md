# AGENTS.md

给在本仓库工作的 AI agent 的指令。本仓库是《从零手写 Agent》学习系列。

## 项目本质(决定一切取舍)

这是一个**学习项目**:产出物是"作者的理解"和"可运行的最小代码",不是产品。任何让代码更"产品化"的建议——更多抽象、配置化、防御性错误处理、扩展性设计——默认都是跑偏,除非当课主题就是它。这类个人项目最常见的死法是基础设施膨胀,本文件的存在就是为了防止它。

## 红线(违反即停下来问作者)

1. **不写规格/设计文档。** 本仓库不允许出现 SPEC.md、DESIGN.md、ROADMAP.md 或任何路线图。计划只允许存在"下一课"。
2. **不建基础设施。** 没有 daemon、web UI、数据库、消息队列、通用框架、插件系统、CI 流水线。只有文件和终端。
3. **每课代码有硬预算。** EP0 ≤ 200 行、单文件、零依赖(不用任何 SDK,直接 HTTP);EP1 ≤ 200 行;EP2 ≤ 1000 行(允许 pi.dev 这一个依赖)。超预算的正确动作是砍功能,不是拆文件规避统计。
4. **不为下一课预留设计。** 每课代码独立自包含;课与课之间允许复制粘贴,禁止提取公共库或 utils。

## 目录与安全约定

- `ep0/`、`ep1/`……每课一个目录:可运行代码 + `notes.md`(作者正文)+ 一屏以内的 README(只写怎么跑)。
- API key 只走环境变量,任何密钥、代理地址不得进入仓库(`.env` 已在 gitignore)。

## 完成定义

一课完成 = 作者亲手跑通代码 + `notes.md` 写完并发布到 blog.aixie.de/notes。代码写完不算完成,agent 跑通不算作者跑通。

## 笔记与实验约定(2026-09-02 全面审核后沉淀)

- **笔记里写的机制,代码里必须真的有,而且按 README 的步骤实际敲过。** 审核时发现 EP3 写了"中途插话",REPL 在 await 期间根本读不到键盘;写了 `replay: safe|never`,没有任何逻辑消费它;EP2 写了"裁判权在宿主",宿主只打印不判。这类"笔记超前于代码"是最难被读者发现、也最伤信任的错。
- **每段实验轨迹标注模型、提供商和日期。** 免费平台的模型随时下线(Groq 于 2026-09 下线 llama-3.3-70b、kimi-k2)。旧轨迹保留为历史记录,复跑另起一节,不覆盖。
- **机制与现象分开写。** 机制不随模型变(tool call 是文本、消息配对、stop 夺回控制权、沙箱归宿主);现象随模型和提供商漂移(编工具名、编 Observation、滥用工具)。复跑只更新现象。
- **代码只认 OpenAI 兼容协议,不绑提供商。** 环境变量是 `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL`(EP2 用 `LLM_PROVIDER` + `LLM_MODEL`),各家配置只写在 `.env.example`,正文不写某个具体免费模型的可用性。
- **行号引用在每次改代码后重算。** 优先用函数名或小节名引用;非用行号不可时,改完代码立刻 grep 一遍 `epN/agent.mjs:` 的全部引用。
- **围栏里的终端记录和模型原话一个字不改。** 它们是证据;要改就重跑再贴。
- **`notes.md` 是源,博客由脚本同步。** 润色只改这里,改完同步到 Lamplight;不要让模型两边分头改。
- **错题本 `questions.md` 编号全局递增、不复用**,每条固定"问题、打勾答案、验证、去向 EP4 第几题"四段,EP4 笔记与 `ep4/quiz.mjs` 按编号引用。
- **让模型润色前先列事实清单**(模型名、次数、哪次实验用了什么提示词、哪些机制是"标注"而非实现),润色后逐条比对。文风可以再润,事实退步读者看不出来。
